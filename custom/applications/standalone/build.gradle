group 'com.cloudcommander.custom.applications.standalone'
version '1.0-SNAPSHOT'

apply plugin: 'com.cloudcommander.module'

dependencies{
    compile project(':vendor:application')
    compile project(':vendor:warehouses')
}

bootRepackage{
    enabled = true
}
