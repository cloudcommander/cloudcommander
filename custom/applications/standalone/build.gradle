group 'com.cloudcommander.custom.applications.standalone'
version '1.0-SNAPSHOT'

apply plugin: 'org.springframework.boot'

dependencies{
    compile project(':vendor:application')

    compile("org.springframework.boot:spring-boot-starter")
}